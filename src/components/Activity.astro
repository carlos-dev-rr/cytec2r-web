---
const {title, date} = Astro.props;
---
<div class="flex flex-col items-center w-[80%] max-w-[300px] bg-white drop-shadow-xl rounded-md my-7 lg:max-w-[400px]">
    <div class="w-full h-[200px]">
        <slot name="img"/>
    </div>
    <div class=" bg-SoftTurquoise text-center p-3 w-full">
        <div class="flex justify-center items-baseline gap-2">
            <i class="fa-solid fa-calendar-days"></i>
            <p>{date}</p>
        </div>
    </div>
    <h3 class="flex-grow text-center text-xl font-bold mt-3 mx-5 lg:text-2xl">{title}</h3>
    <div class="p-5 text-justify hidden">
        <slot name="description"/>
    </div>
    <button class="bg-LightSkyBlue border-1 border-sky-200 text-sm text-sky-950 px-4 py-1 my-5 cursor-pointer font-bold rounded-full w-1/2">Más información</button>
</div>